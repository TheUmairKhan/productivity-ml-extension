{
  "name": "mlops",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@types/chrome": "^0.1.32",
    "typescript": "^5.0.0",
    "vitest": "^3.2.4"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0"
  }
}
